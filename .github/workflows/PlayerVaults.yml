name: PlayerVaults

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/PlayerVaults.yml'
  schedule:
    - cron: '0 */12 * * *'

jobs:
  PlayerVaults:
    uses: AquaMCVN/ConstructProjects/.github/workflows/maven-build.yml@main
    with:
      repository: "drtshock/PlayerVaults"
      name: "PlayerVaults"
      target: "target/*"
      java: 17

